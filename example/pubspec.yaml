name: example
description: "A demo project for the crop_image_widget package."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crop_image_widget:
    path: ../
  cupertino_icons: any
  image_picker: any
  web: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any

flutter:
  uses-material-design: true
  assets:
    - assets/bowl.jpg
    - assets/potato.jpg
